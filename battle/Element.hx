package battle;


enum Element
{

	FIRE(power:Float);
	WATER(power:Float);
	EARTH(power:Float);
	WIND(power:Float);
	LIGHT(power:Float);
	DARK(power:Float);
	
	SLASH(power:Float);
	THRUST(power:Float);
	STRIKE(power:Float);
	
}